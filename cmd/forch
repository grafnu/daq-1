#!/bin/bash -e

if [ -z "$1" ]; then
    SOCK=inst/faucet_event.sock
else
    SOCK=inst/faucet/daq-faucet-$1/faucet_event.sock
fi

if [ ! -z "$2" ]; then
    CPN_FILE="$2"
fi

echo Using event socket $SOCK
sudo FAUCET_EVENT_SOCK=$SOCK CPN_YAML_FILE=$CPN_FILE venv/bin/python daq/forchestrator.py
