site:
  name: nz-kiwi
processes:
  faucet:
    regex: python.*ryu-manager.*faucet\.faucet
  gauge:
    regex: python.*ryu-manager.*faucet\.gauge
  keepalived:
    proc_name: keepalived
    regex: keepalived
  forch:
    regex: ^python.*forchestrator
  bosun:
    regex: ddunsel_watcher.*bosun
